    // This is a zero-sized patch field created to
    // facilitate parallel mesh distribution.
    // Processor faces are temporarily added to the last
    // non-empty, non-coupled patch during this process,
    // and this causes potential difficulties for derived
    // patch types (specifically, mixed).

    redistributeHelper
    {
        #include    "helperPatchFieldType"
        value       $internalField;
    }

